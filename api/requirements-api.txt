# 公式识别 API 依赖
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0
python-dotenv==1.0.0

# 现有项目依赖
pillow==10.1.0
latex2mathml==3.77.0
pix2tex

# PyTorch 安装说明：
#   - 方案 A（pytorch 官方镜像）：torch 已由基础镜像提供，无需安装（注释掉下面一行）
#   - 方案 B（python:3.11-slim 基础镜像）：需要安装 CPU 版 PyTorch
torch --index-url https://download.pytorch.org/whl/cpu    # CPU 版 PyTorch（约 ~200MB）
numpy<2  # pix2tex 需要 NumPy 1.x